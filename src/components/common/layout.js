/* eslint-disable react/prop-types */
import DefaultLayout from '../DefaultLayout';

export default function Layout({ children }) {
  return <DefaultLayout title="Next Salat">{children}</DefaultLayout>;
}
